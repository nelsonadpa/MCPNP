{
  "name": "jamaica-ereg-tests",
  "version": "1.0.0",
  "description": "E2E tests for Jamaica eRegistrations — Establish a new zone",
  "scripts": {
    "auth": "npx playwright test --project=auth-setup --headed --config=playwright.config.ts",
    "test": "npx playwright test --config=playwright.config.ts",
    "test:headed": "npx playwright test --headed --config=playwright.config.ts",
    "test:frontoffice": "npx playwright test --project=jamaica-frontoffice --headed --config=playwright.config.ts",
    "test:bpa": "npx playwright test --project=jamaica-bpa --headed --config=playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
